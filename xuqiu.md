请以仿 Discord 的纯文字聊天 Web 应用为目标，为我设计一套完整的开发方案 + 可运行的代码框架，技术栈严格限定为Python 做后端 + Vue3 做前端，不开发语音 / 视频功能，仅实现文字聊天核心需求，要求贴合 Discord 的核心产品逻辑，同时适配 Web 端的操作体验，具体输出要求如下：
先梳理核心功能清单（贴合 Discord 核心，剔除语音相关，聚焦文字聊天 + 社群管理）；
再设计技术架构选型（Python 后端明确框架 / 数据库 / 通信协议，Vue3 前端明确 UI 框架 / 状态管理 / 通信方式，必须适配纯文字实时聊天）；
输出后端（Python）核心代码（含项目结构、接口编写、实时通信实现、数据模型设计，带详细注释）；
输出前端（Vue3）核心代码（含项目结构、页面布局、实时聊天组件、交互逻辑，贴合 Discord 的界面层级，带详细注释）；
补充前后端联调步骤+本地运行环境配置要求，保证新手能按步骤启动项目；
界面布局贴合 Discord 的经典三栏式（服务器导航栏 + 频道列表栏 + 聊天内容栏），仅做文字聊天适配，剔除语音频道、语音通话等所有语音相关模块；
核心交互需实现 Discord 的核心文字逻辑：实时文字发送 / 接收、消息气泡展示、频道切换、服务器 / 频道创建、简单的用户登录 / 注册。
精简版提示词（若 AI 输出内容过长，用这个压缩需求，核心不变）
帮我开发一个仿 Discord 的纯文字聊天 Web 应用，技术栈为Python 后端 + Vue3 前端，无任何语音功能，要求如下：
贴合 Discord 三栏式界面（服务器栏 + 频道栏 + 聊天栏），仅实现文字相关功能；
输出 Python 后端（含实时通信、接口、数据库）和 Vue3 前端（含页面、聊天组件、实时交互）的完整代码框架，带注释；
实现核心功能：用户登录注册、服务器 / 频道创建、实时文字聊天、消息展示、频道切换；
补充环境配置和本地运行步骤，保证可直接启动。
专属优化提示词（若需要 AI补充功能 / 优化代码，后续可直接加这个）
基于你之前输出的 QuickLink（仿 Discord 文字聊天 Web，Python+Vue3）代码，帮我【XXX】（替换为需求，如：优化消息实时推送、增加消息撤回功能、美化聊天界面、补充用户资料编辑功能），要求保持原技术栈和三栏式布局，不增加语音功能。
关键开发提醒（AI 输出后可重点核对）
实时文字聊天的核心是WebSocket 协议（Python 后端推荐用 FastAPI+WebSocket/Flask-SocketIO，Vue3 前端用 socket.io-client），这是仿 Discord 实时性的关键，AI 输出时需确认包含该技术；
数据库推荐轻量易上手的MySQL/PostgreSQL（生产环境）或SQLite（本地测试，无需额外安装，新手友好）；
Vue3 前端推荐搭配Element Plus/Naive UI做 UI 组件（贴合 Web 端，开发效率高），AI 输出通常会自带组件库选型。